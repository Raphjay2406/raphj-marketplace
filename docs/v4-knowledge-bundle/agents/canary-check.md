---
id: "genorah/canary-check"
name: "canary-check"
tier: "protocol"
version: "4.0.0"
channel: "stable"
capabilities: "Canary protocol for detecting context drift across agent invocations — flags DNA/archetype deviation early"
source: "agents/protocols/canary-check.md"
tags: [agent, genorah, protocol]
---

# canary-check

> Canary protocol for detecting context drift across agent invocations — flags DNA/archetype deviation early.

## Preview

# Canary Check Protocol  > Protocol document for the Genorah 2.0 build pipeline. Executed by the build-orchestrator after every wave. Detects context rot before it produces bad output by testing the

## Frontmatter

```yaml
name: canary-check
description: "Canary protocol for detecting context drift across agent invocations — flags DNA/archetype deviation early."
```
