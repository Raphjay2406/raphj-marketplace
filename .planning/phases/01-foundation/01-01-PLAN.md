---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - CLAUDE.md
  - skills/_skill-template/SKILL.md
autonomous: true

must_haves:
  truths:
    - "Plugin manifest declares Modulo 2.0 with correct metadata"
    - "CLAUDE.md accurately describes the 2.0 architecture, commands, and workflow"
    - "A skill template file demonstrates the 4-layer format so all subsequent skills follow it"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with name, version 2.0.0, description, author"
      contains: "modulo"
    - path: "CLAUDE.md"
      provides: "Project overview, architecture, workflow, key concepts, modification guide"
      min_lines: 80
    - path: "skills/_skill-template/SKILL.md"
      provides: "4-layer skill format template with YAML frontmatter"
      contains: "Layer 1: Decision Guidance"
  key_links:
    - from: "CLAUDE.md"
      to: "skills/_skill-template/SKILL.md"
      via: "Architecture section references 4-layer format"
      pattern: "4-layer"
---

<objective>
Create the plugin skeleton for Modulo 2.0: updated manifest, rewritten CLAUDE.md, and the 4-layer skill format template.

Purpose: Every subsequent plan references the conventions established here -- file locations, skill format, naming conventions, architecture description. This is the foundation that all other plans build on.
Output: Updated plugin.json, rewritten CLAUDE.md, skill template file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update plugin manifest and rewrite CLAUDE.md</name>
  <files>.claude-plugin/plugin.json, CLAUDE.md</files>
  <action>
**plugin.json:** Update the existing manifest:
- `version`: "2.0.0"
- `name`: "modulo"
- `description`: Update to reflect 2.0 -- mention machine-enforceable design identity, 3-tier skill system (Core/Domain/Utility), 4-layer skill format, and pipeline architecture. Remove specific counts like "87 skills" since those will change. Keep the PreToolUse hook configuration as-is (DNA compliance check).
- `author`: keep "raphj"

**CLAUDE.md:** Rewrite completely for 2.0. Must be under 150 lines total (per RESEARCH.md pitfall #5). Structure:

1. **Project Overview** (~10 lines): Modulo 2.0, Claude Code plugin for premium frontend design. NOT a template generator. Machine-enforceable identity systems. Mention the key systems: Design DNA, Design Archetypes (19 including Neubrutalism, Dark Academia, AI-Native), Emotional Arc, Anti-Slop Gate. Markdown-only repo (no app code, no build system, no tests).

2. **Architecture** (~20 lines): Three-tier system diagram (commands -> agents -> skills). Plugin manifest at `.claude-plugin/plugin.json`. File conventions:
   - Skills: `skills/{skill-name}/SKILL.md` (4-layer format, YAML frontmatter)
   - Agents: `agents/{agent-name}.md` (Phase 2+)
   - Commands: `commands/{command-name}.md` (Phase 3+)
   - Hooks: `.claude-plugin/hooks/` (DNA compliance)

3. **Skill Tiers** (~15 lines): Core (always loaded, ~6 skills: design-dna, design-archetypes, anti-slop-gate, emotional-arc, typography, color-system), Domain (loaded per project), Utility (on-demand). 4-layer format: Decision Guidance, Award-Winning Examples, Integration Context, Anti-Patterns.

4. **Core Workflow** (~15 lines): The 5-command pipeline:
   `/modulo:start-project` -> `/modulo:lets-discuss` -> `/modulo:plan-dev` -> `/modulo:execute` -> `/modulo:iterate`
   Brief description of each. Note commands are being redesigned from v6.1.0's 13 commands.

5. **Key Concepts** (~30 lines, one paragraph each -- NOT full definitions):
   - Design DNA (12 color tokens: 8 semantic + 4 expressive, type scale, spacing, signature element, motion language)
   - Design Archetypes (19 archetypes with locked constraints, escape hatch via tension override)
   - Creative Tension (5 tension types, per-archetype techniques, 1-3 per page)
   - Emotional Arc (10 beats with hard parameter constraints)
   - Anti-Slop Gate (35-point weighted scoring, 7 categories, post-review enforcement, named tiers)
   - Awwwards 4-Axis Scoring (Design/Usability/Creativity/Content, SOTD 8.0+ target)
   - Wave System (Wave 0 scaffold, Wave 1 shared UI, Wave 2+ parallel sections)

6. **Managed Artifacts** (~10 lines): Table of files created in target projects under `.planning/modulo/`

7. **Modifying This Plugin** (~10 lines): How to add skills, agents, commands. Mention version bump and README update.

IMPORTANT constraints:
- Do NOT duplicate content that belongs in skills. If it needs more than 2 sentences, it belongs in a skill.
- Use present tense throughout ("Modulo creates" not "Modulo will create")
- Reference the actual numbers: 19 archetypes, 12 color tokens, 35-point gate, 10 beat types
  </action>
  <verify>
- `cat .claude-plugin/plugin.json | python -m json.tool` succeeds (valid JSON)
- CLAUDE.md exists and has fewer than 150 lines
- CLAUDE.md contains "2.0" and "4-layer"
- CLAUDE.md mentions all 6 key concepts
  </verify>
  <done>
- plugin.json shows version 2.0.0 with updated description
- CLAUDE.md is a concise (<150 line) project guide that downstream agents and builders can reference for conventions
- No skill-level detail leaks into CLAUDE.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 4-layer skill format template</name>
  <files>skills/_skill-template/SKILL.md</files>
  <action>
Create `skills/_skill-template/SKILL.md` as the canonical template for all skills. This is NOT a real skill (the `_` prefix signals it is a template). It should be a fillable template with:

**YAML Frontmatter:**
```yaml
---
name: "[skill-name]"
description: "[One-line: what this skill provides]"
tier: "[core | domain | utility]"
triggers: "[comma-separated phrases that activate this skill]"
version: "2.0.0"
---
```

**Layer 1: Decision Guidance** (~placeholder instructions):
- When to use this skill (conditions/triggers)
- When NOT to use (redirect to other skills)
- Decision tree for common choices
- Connection to pipeline stages (which agents/commands reference this)

**Layer 2: Award-Winning Examples** (~placeholder instructions):
- Code Patterns section: copy-paste TSX blocks for the 2-3 most common patterns
- Reference Sites section: 3-5 award-winning sites with annotations on what makes them excellent
- Note: Code shows "how", references show "why"

**Layer 3: Integration Context** (~placeholder instructions):
- DNA Connection: which DNA tokens this skill maps to
- Archetype Variants: how behavior changes per archetype
- Pipeline Stage: where in the build pipeline this skill is consumed
- Related Skills: what other skills this connects to

**Layer 4: Anti-Patterns** (~placeholder instructions):
- 3-5 anti-patterns with structure: Name, What goes wrong, Instead (correct approach)
- Common mistakes specific to this skill's domain

**Machine-Readable Constraint Block** (optional section, placeholder):
- Show the table format for enforceable constraints: Parameter | Min | Max | Unit | Enforcement
- Note: only skills with enforceable parameters include this section

The template should be ~60-80 lines total. Each section should have brief guidance comments wrapped in `<!-- -->` HTML comments explaining what goes there, plus a minimal example showing the FORMAT (not real content).
  </action>
  <verify>
- `skills/_skill-template/SKILL.md` exists
- File contains all 4 layer headings: "Layer 1: Decision Guidance", "Layer 2: Award-Winning Examples", "Layer 3: Integration Context", "Layer 4: Anti-Patterns"
- File starts with YAML frontmatter (--- delimiters)
- File contains the constraint table format example
  </verify>
  <done>
- The template is a self-documenting format reference that any plan executor can follow to produce consistent skills
- All 4 layers are represented with placeholder guidance
- YAML frontmatter includes all required fields (name, description, tier, triggers, version)
  </done>
</task>

</tasks>

<verification>
- `plugin.json` is valid JSON with version "2.0.0"
- CLAUDE.md is under 150 lines and covers all architectural conventions
- Skill template exists and demonstrates the complete 4-layer format
- No v6.1.0 content remains in CLAUDE.md (rewritten, not patched)
- Directory structure is clean: `.claude-plugin/plugin.json`, `skills/_skill-template/SKILL.md`
</verification>

<success_criteria>
1. Any Claude executor reading CLAUDE.md understands the project structure, file conventions, and key concepts without needing to read any skill files
2. Any Claude executor creating a new skill can follow the template to produce a correctly structured 4-layer SKILL.md
3. plugin.json correctly identifies this as Modulo 2.0.0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
