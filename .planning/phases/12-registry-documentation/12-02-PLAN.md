---
phase: 12-registry-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README.md documents exactly 8 commands with correct v2.0 names"
    - "README.md lists 19 design archetypes (not 16)"
    - "README.md documents the v2.0 pipeline agent structure (7 pipeline + 4 protocols + 3 specialists)"
    - "README.md version matches plugin.json (2.0.0-dev)"
    - "README.md workflow sequence is start-project -> lets-discuss -> plan-dev -> execute -> iterate"
    - "README.md framework support lists Next.js, Astro, React/Vite, Tauri, and Electron"
  artifacts:
    - path: "README.md"
      provides: "Project documentation matching v2.0 architecture"
      contains: "2.0.0-dev"
  key_links:
    - from: "README.md"
      to: "commands/*.md"
      via: "Command names listed in README match actual command files"
      pattern: "/gen:"
    - from: "README.md"
      to: ".claude-plugin/plugin.json"
      via: "Version string matches"
      pattern: "2\\.0\\.0-dev"
---

<objective>
Rewrite README.md to document the v2.0 architecture, commands, and workflow.

Purpose: README.md currently contains v6.1.0 content -- wrong commands (13 vs 8), wrong agents (17 vs pipeline structure), wrong archetypes (16 vs 19), wrong workflow sequence, and wrong framework support. This is the public face of the plugin and must accurately describe v2.0. This closes audit ISSUE-5 (MAJOR).

Output: Rewritten `README.md` reflecting the complete v2.0 system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-registry-documentation/12-RESEARCH.md
@README.md
@CLAUDE.md
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for v2.0</name>
  <files>README.md</files>
  <action>
Write README.md from scratch. Do NOT patch the existing v6.1.0 content -- the delta is too large. Use CLAUDE.md as the authoritative reference for architecture and naming, and the research file for exact inventories.

**README.md structure (in order):**

1. **Title and description:**
   `# genorah` followed by a concise description: premium frontend design plugin for Claude Code, Awwwards SOTD 8.0+ baseline, NOT a template generator. Mention: 45+ skills (v2.0), 8 commands, pipeline agent architecture, 19 archetypes, Design DNA, creative tension, emotional arc, 35-point anti-slop gate. Works with Next.js, Astro, React/Vite, Tauri, and Electron. Version: 2.0.0-dev.

2. **Installation:**
   Keep existing marketplace installation instructions (they are correct):
   ```
   claude plugin marketplace add https://github.com/Raphjay2406/raphj-marketplace
   claude plugin install genorah@raphj-marketplace
   ```

3. **What Makes Genorah Different:**
   Brief section (NOT the v6.0/v6.1 "what's new" sections). Cover:
   - Every project gets a unique visual identity via Design DNA (not templates)
   - 19 opinionated archetypes with machine-enforceable constraints
   - 35-point anti-slop quality gate with named tiers
   - Emotional arc storytelling (10 beat types with hard constraints)
   - Creative tension system for controlled rule-breaking
   - Cinematic motion with DNA-generated presets
   - Pipeline agent architecture (stateless builders, context rot prevention)

4. **Commands (8):**
   Table format:
   ```
   | Command | Description |
   |---------|-------------|
   ```

   Commands (from `commands/*.md`):
   - `/gen:start-project` -- Start a new project: discovery, parallel research, archetype selection, Design DNA generation, content planning
   - `/gen:lets-discuss` -- Per-phase creative deep dive with visual feature proposals, brand voice refinement
   - `/gen:plan-dev` -- Create section plans with wave assignments, re-research, verification questions
   - `/gen:execute` -- Build sections wave by wave with parallel builders and session management
   - `/gen:iterate` -- Brainstorm-first design improvements with user approval before changes
   - `/gen:bug-fix` -- Diagnostic root cause analysis with proposed solutions
   - `/gen:status` -- Show full project status
   - `/gen:audit` -- Comprehensive site audit (visual, performance, accessibility, content)

5. **Workflow:**
   ASCII diagram:
   ```
   start-project -> lets-discuss -> plan-dev -> execute -> iterate (if needed)
                                                        -> audit (optional)
                                                        -> bug-fix (as needed)
   ```
   Then 1-2 sentences per step describing what happens. Reference that `status` is available at any point.

6. **Pipeline Agents:**
   Three sub-sections:

   **Pipeline Agents** (table with 7 entries from `agents/pipeline/`):
   researcher, creative-director, section-planner, build-orchestrator, section-builder, quality-reviewer, polisher

   **Protocols** (table with 4 entries from `agents/protocols/`):
   discussion-protocol, context-rot-prevention, canary-check, agent-memory-system

   **Domain Specialists** (table with 3 entries from `agents/specialists/`):
   3d-specialist, animation-specialist, content-specialist

7. **Design DNA System:**
   Based on CLAUDE.md. Cover: 12 color tokens (8 semantic + 4 expressive), display/body/mono fonts, 8-level type scale, 5-level spacing, signature element, 8+ motion tokens. Mention Tailwind v4 `@theme` CSS generation.

8. **Design Archetypes (19 + Custom):**
   Table listing ALL 19:
   Brutalist, Ethereal, Kinetic, Editorial, Neo-Corporate, Organic, Retro-Future, Luxury/Fashion, Playful/Startup, Data-Dense, Japanese Minimal, Glassmorphism, Neon Noir, Warm Artisan, Swiss/International, Vaporwave, **Neubrutalism**, **Dark Academia**, **AI-Native**

   Include personality column and 1-2 reference examples per archetype. Mention custom archetype builder.

9. **Anti-Slop Gate:**
   Keep the 7-category table (it was mostly correct in the old README). Update scoring text: "Score 25+ = Pass, 28+ = Strong, 30+ = SOTD-Ready, 33+ = Honoree-Level". Mention penalties.

10. **Wave System:**
    Keep Wave 0/1/2+ structure (was correct). Mention max 4 builders per wave.

11. **Skills:**
    Compact summary referencing SKILL-DIRECTORY.md for full details:
    - 3-tier loading (Core/Domain/Utility)
    - Core (~21): identity, quality, motion, brainstorming -- always loaded
    - Domain (~22): frameworks, assets, verticals -- per project
    - Utility (3): form, SEO, i18n -- on demand
    - All v2.0 skills use 4-layer format
    - Reference `skills/SKILL-DIRECTORY.md` for complete registry

12. **Planning Artifacts:**
    Table of artifacts under `.planning/genorah/`:
    PROJECT.md, DESIGN-DNA.md, BRAINSTORM.md, MASTER-PLAN.md, CONTEXT.md, STATE.md, sections/*/PLAN.md, sections/*/SUMMARY.md

13. **Framework Support:**
    Table with 5 frameworks:
    - Next.js (App Router + Pages Router)
    - Astro (Islands, Content Layer, ClientRouter)
    - React/Vite (SPA patterns)
    - Tauri (desktop-aware, custom titlebars)
    - Electron (desktop-aware, window chrome)

14. **License:** MIT

**CRITICAL constraints:**
- Version MUST be `2.0.0-dev` (from plugin.json), NOT `2.0.0`
- Command count MUST be 8 (not 13)
- Archetype count MUST be 19 (not 16)
- Agent section MUST use pipeline/protocols/specialists structure (not flat list of 17)
- Framework support MUST include React/Vite, Tauri, Electron (not just Next.js + Astro)
- Do NOT include v6.0/v6.1 "What's New" sections -- those are obsolete
- Do NOT list individual legacy skills
- Do NOT reference commands that don't exist (start-design, plan-sections, verify, visual-audit, responsive-check, generate-tests, lighthouse, update, change-plan, export)
  </action>
  <verify>
Run these verification checks:

1. Grep for "2.0.0-dev" in README.md -- must appear
2. Count command entries in the Commands table -- must be exactly 8
3. Grep for "start-design" -- must NOT appear
4. Grep for "plan-sections" -- must NOT appear
5. Grep for "17 agents" or "13 commands" or "87 skills" -- must NOT appear
6. Grep for "16 archetypes" -- must NOT appear
7. Verify "Neubrutalism", "Dark Academia", "AI-Native" all appear
8. Verify "Tauri" and "Electron" appear in framework support
9. Verify "React/Vite" or "React" + "Vite" appear in framework support
10. Grep for "v6.1" or "v6.0" -- must NOT appear (no legacy version references)
  </verify>
  <done>
README.md accurately documents v2.0: 8 commands, 19 archetypes, pipeline agent structure, 5 framework targets, version 2.0.0-dev. Zero references to legacy v6.1.0 commands, agents, or counts.
  </done>
</task>

</tasks>

<verification>
1. README.md version matches plugin.json (2.0.0-dev)
2. Exactly 8 commands documented
3. 19 archetypes listed
4. Pipeline agent structure (7 + 4 + 3) documented
5. 5 frameworks supported
6. Zero legacy references (start-design, plan-sections, verify, 17 agents, 13 commands, 87 skills)
7. Workflow sequence matches: start-project -> lets-discuss -> plan-dev -> execute -> iterate
</verification>

<success_criteria>
- README.md is a complete, accurate document of the v2.0 system
- No v6.1.0 content remains
- Audit ISSUE-5 (MAJOR) is fully resolved
</success_criteria>

<output>
After completion, create `.planning/phases/12-registry-documentation/12-02-SUMMARY.md`
</output>
