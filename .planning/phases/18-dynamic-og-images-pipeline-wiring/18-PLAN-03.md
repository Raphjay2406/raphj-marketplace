---
phase: 18-dynamic-og-images-pipeline-wiring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/pipeline/section-planner.md
  - agents/pipeline/quality-reviewer.md
  - agents/pipeline/build-orchestrator.md
  - agents/pipeline/researcher.md
  - agents/specialists/content-specialist.md
  - skills/design-system-scaffold/SKILL.md
autonomous: true

must_haves:
  truths:
    - "Section-planner assigns structured-data schemas and OG template type in per-section PLAN.md files"
    - "Quality-reviewer has an SEO verification checklist with CRITICAL and WARNING severity levels"
    - "Build-orchestrator Wave 0 scaffold includes SEO files (sitemap.ts, robots.ts, metadata base, OG route)"
    - "Researcher agent has Context7 MCP tools (resolve-library-id, query-docs) in its tools list and usage guidance"
    - "Content-specialist references GEO content patterns (BLUF, question headings, quotable statistics) for search-visible content"
    - "Design-system-scaffold Wave 0 task list includes SEO scaffold items"
  artifacts:
    - path: "agents/pipeline/section-planner.md"
      provides: "Schema assignment and OG template type in PLAN.md generation"
      contains: "structured-data"
    - path: "agents/pipeline/quality-reviewer.md"
      provides: "SEO verification checklist section"
      contains: "SEO Verification"
    - path: "agents/pipeline/build-orchestrator.md"
      provides: "SEO scaffold items in Wave 0"
      contains: "sitemap.*robots.*OG"
    - path: "agents/pipeline/researcher.md"
      provides: "Context7 MCP tool integration"
      contains: "mcp__context7"
    - path: "agents/specialists/content-specialist.md"
      provides: "GEO content pattern references"
      contains: "GEO.*BLUF"
    - path: "skills/design-system-scaffold/SKILL.md"
      provides: "SEO scaffold files in Wave 0 manifest"
      contains: "sitemap.ts.*robots.ts"
  key_links:
    - from: "agents/pipeline/section-planner.md"
      to: "skills/structured-data/SKILL.md"
      via: "Schema assignment instruction referencing Per-Page-Type Recipe Table"
      pattern: "structured-data.*schema"
    - from: "agents/pipeline/build-orchestrator.md"
      to: "skills/seo-meta/SKILL.md"
      via: "Wave 0 SEO scaffold file references"
      pattern: "seo-meta.*og-images"
    - from: "agents/pipeline/researcher.md"
      to: "Context7 MCP tools"
      via: "Tool list addition with usage guidance"
      pattern: "context7.*resolve-library-id"
---

<objective>
Wire all v1.5 skills (seo-meta, structured-data, search-visibility, og-images) into the agent pipeline by making targeted additions to 5 agent files and 1 skill file.

Purpose: Currently no pipeline agent references structured-data, search-visibility, or OG image skills. After this plan, every v1.5 skill activates automatically at the correct pipeline stage: section-planner assigns schemas, quality-reviewer checks SEO, build-orchestrator scaffolds SEO files, researcher uses Context7 for live docs, and content-specialist applies GEO patterns. All changes are ADDITIVE -- no existing behavior is removed or modified.

Output: Targeted additions to `section-planner.md`, `quality-reviewer.md`, `build-orchestrator.md`, `researcher.md`, `content-specialist.md`, and `design-system-scaffold/SKILL.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dynamic-og-images-pipeline-wiring/18-CONTEXT.md
@.planning/phases/18-dynamic-og-images-pipeline-wiring/18-RESEARCH.md
@agents/pipeline/section-planner.md
@agents/pipeline/quality-reviewer.md
@agents/pipeline/build-orchestrator.md
@agents/pipeline/researcher.md
@agents/specialists/content-specialist.md
@skills/design-system-scaffold/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire section-planner, quality-reviewer, and build-orchestrator</name>
  <files>agents/pipeline/section-planner.md, agents/pipeline/quality-reviewer.md, agents/pipeline/build-orchestrator.md</files>
  <action>
Make ADDITIVE changes to three agent files. Read each file first, then make targeted insertions. Do NOT rewrite or restructure existing content.

**A. section-planner.md -- Add structured-data schema assignment and OG template type:**

1. In the "Skill reference" area (after the existing `copy-intelligence` skill reference near line 20-21), add a NEW skill reference line:
   ```
   **Skill reference:** Load `skills/structured-data/SKILL.md` for the Per-Page-Type Recipe Table when assigning JSON-LD schemas to section plans. Consult the recipe table to select the correct schema type (FAQPage, Article, Product, LocalBusiness, etc.) based on the section's content purpose.
   ```

2. In the "Per-Section PLAN.md Format" section (around the frontmatter template, lines 115-131), add two new fields to the frontmatter block:
   ```yaml
   schema_type: [FAQPage | Article | Product | LocalBusiness | HowTo | Organization | BreadcrumbList | none]
   og_template: [article | landing | product | auto]
   ```
   Add these after the `must_haves` field in the example frontmatter.

3. After the "Per-Section PLAN.md Format" frontmatter section and before the "Body sections" part, add a brief note:
   ```markdown
   **SEO fields:** `schema_type` is determined by consulting the `structured-data` skill's Per-Page-Type Recipe Table based on section content purpose (FAQ section = FAQPage, blog post = Article, etc.). Set to `none` if no schema applies. `og_template` defaults to `auto` (convention-based detection from route path) unless the section requires an explicit type override.
   ```

4. In the "Planning Process" section, at the end of "Step 6: Generate Per-Section PLAN.md Files" (around line 372-380), add a sub-step:
   ```markdown
   3. Assign `schema_type` from the structured-data skill's recipe table based on section content
   4. Assign `og_template` (default `auto`, override only if route convention would pick wrong type)
   ```

**B. quality-reviewer.md -- Add SEO verification checklist:**

1. After the "Three-Level Goal-Backward Verification" section and BEFORE the "Anti-Slop 35-Point Scoring" section (between lines 90 and 92), insert a new section:

   ```markdown
   ---

   ## SEO Verification Checklist

   After Level 3 (Wired) verification, run the SEO checklist on every public-facing page. This is ADVISORY -- SEO items do not block the anti-slop gate. Items are classified by severity.

   ### CRITICAL (must fix before ship)

   - [ ] Every page has a `<title>` tag (Next.js: in generateMetadata, Astro: in <head>)
   - [ ] Every page has a canonical URL (not hardcoded, generated from route)
   - [ ] Every page has an `og:image` (via opengraph-image.tsx file convention or explicit meta tag)
   - [ ] JSON-LD schema syntax is valid (no broken brackets, all required fields present)
   - [ ] No duplicate `<title>` tags across different pages
   - [ ] robots.txt exists and references sitemap URL

   ### WARNING (recommended fix, not blocking)

   - [ ] Meta description length is 120-160 characters
   - [ ] og:image alt text is descriptive (not "image" or empty)
   - [ ] Schema type matches page content (FAQPage on FAQ section, Article on blog, etc.)
   - [ ] Heading hierarchy is sequential (H1 -> H2 -> H3, no skipped levels)
   - [ ] Images have descriptive alt text (not just "photo" or "image")

   ### How to Use

   Run this checklist AFTER the 3-level verification and BEFORE anti-slop scoring. Report SEO findings in the verification report under a separate "SEO Status" heading. CRITICAL items should be included in GAP-FIX.md if they fail. WARNING items are logged but do not generate GAP-FIX entries.

   **Skill references for SEO validation values:** Consult `seo-meta` skill constraint table for title/description length ranges. Consult `structured-data` skill for valid schema types per page type.
   ```

**C. build-orchestrator.md -- Add SEO scaffold items to Wave 0:**

1. In the "Step 3: Read Current Wave PLANs" area, after the existing step 3 content (around line 68-70), add a note for Wave 0 specifically:

   ```markdown
   **Wave 0 SEO scaffold:** When executing Wave 0, the scaffold builder also generates SEO foundation files alongside design tokens. Reference the `seo-meta` and `og-images` skills for scaffold patterns. SEO scaffold items:
   - `app/sitemap.ts` or `@astrojs/sitemap` config (sitemap generation)
   - `app/robots.ts` or `public/robots.txt` (crawler directives)
   - Metadata base configuration in root layout (metadataBase for Next.js)
   - `app/opengraph-image.tsx` root-level default OG image (Next.js) or `src/pages/og/` directory (Astro)
   ```

2. In the "Complete Build Context Spawn Prompt Template" section, after the "Quality Rules" subsection (around lines 318-343), add a brief SEO context block that the orchestrator includes in spawn prompts:

   ```markdown
   ### SEO Context (include for content sections)
   Schema type: [from PLAN.md frontmatter schema_type field, or "none"]
   OG template: [from PLAN.md frontmatter og_template field, or "auto"]
   If schema_type is set: Builder should include appropriate JSON-LD script tag per structured-data skill patterns.
   ```
  </action>
  <verify>
Read all three modified files. Confirm:
1. section-planner.md has structured-data skill reference near existing copy-intelligence reference
2. section-planner.md PLAN.md frontmatter includes schema_type and og_template fields
3. section-planner.md has SEO fields explanation note
4. section-planner.md Planning Process Step 6 includes schema and OG assignment sub-steps
5. quality-reviewer.md has SEO Verification Checklist section between Level 3 and Anti-Slop scoring
6. quality-reviewer.md SEO checklist has CRITICAL items (title, canonical, og:image, JSON-LD, robots.txt)
7. quality-reviewer.md SEO checklist has WARNING items (description length, alt text, schema match)
8. build-orchestrator.md has Wave 0 SEO scaffold note with 4 items (sitemap, robots, metadata base, OG route)
9. build-orchestrator.md spawn prompt template has SEO Context block
10. All changes are ADDITIVE -- no existing content removed or restructured
  </verify>
  <done>Three core pipeline agents now reference v1.5 SEO skills: section-planner assigns schemas and OG templates, quality-reviewer has an SEO checklist, and build-orchestrator scaffolds SEO files in Wave 0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire researcher, content-specialist, and design-system-scaffold</name>
  <files>agents/pipeline/researcher.md, agents/specialists/content-specialist.md, skills/design-system-scaffold/SKILL.md</files>
  <action>
Make ADDITIVE changes to three files. Read each file first, then make targeted insertions.

**A. researcher.md -- Add Context7 MCP tools:**

1. In the frontmatter, update the tools line to add Context7:
   ```yaml
   tools: Read, Write, Grep, Glob, WebSearch, WebFetch, mcp__context7__resolve-library-id, mcp__context7__query-docs
   ```

2. After the "Research Process" Step 2 (around line 133-138 where it says "Use WebSearch and WebFetch to gather information"), insert a new sub-step before the existing WebSearch guidance:

   ```markdown
   **Context7 MCP Integration (use FIRST for library documentation):**

   Before using WebSearch for library-specific questions, query Context7 for up-to-date documentation:

   1. **Resolve the library:** Use `mcp__context7__resolve-library-id` with the library name to get its Context7 identifier
      - Example: resolve "next.js" -> get library ID for Next.js documentation
   2. **Query documentation:** Use `mcp__context7__query-docs` with the library ID and your specific question
      - Example: query Next.js docs for "generateMetadata async params" to get current v16 syntax
   3. **Fallback chain:** Context7 (fastest, most current) -> Official docs via WebFetch -> WebSearch (broadest, least current)

   Context7 provides library documentation that is more current than training data. Use it for:
   - Framework API syntax verification (Next.js, Astro, React)
   - Library version-specific features and breaking changes
   - Configuration syntax and options
   - Migration guides between versions
   ```

3. In the "Rules" section at the bottom, add one new rule:
   ```markdown
   - **Context7 first for libraries.** When researching library-specific patterns (API syntax, configuration options, version differences), query Context7 before WebSearch. Context7 provides official documentation that is more current than web search results.
   ```

**B. content-specialist.md -- Add GEO content pattern awareness:**

1. After the "Content Domain Knowledge (SPECIALIST EXCLUSIVE)" header and before "Brand Voice Enforcement" (around line 79), insert a new subsection:

   ```markdown
   ### GEO Content Patterns (Search Visibility)

   When building content-heavy sections for public-facing sites targeting search visibility, apply GEO (Generative Engine Optimization) patterns that help content appear in AI engine citations without destroying premium aesthetics.

   **GEO Pattern Triggers:**
   - Section is content-heavy (blog feature, case study, about page, FAQ)
   - Project targets organic search traffic or AI engine visibility
   - PLAN.md has `schema_type` set (indicates SEO-aware section)

   **Patterns to Apply (when triggered):**
   - **BLUF (Bottom Line Up Front):** Lead with the key takeaway in the first sentence/paragraph, not buried after introduction. AI engines extract the first substantive statement
   - **Question-Based Headings:** Use H2/H3 headings phrased as questions that users actually search for. "How does X work?" not "Our Approach"
   - **Quotable Statistics:** Format key numbers with context and citation. "Projects using DNA tokens ship 40% faster (Internal benchmark, 2025)" -- the citation format helps AI engines treat it as a citable fact
   - **FAQ-First Content:** If the section has FAQ content, structure it with the question as a heading and a concise answer as the first paragraph, followed by supporting detail

   **Archetype-Aware GEO:** GEO patterns must NOT override the archetype's voice. A Brutalist project's FAQ should still feel raw and direct. An Ethereal project's statistics should still feel elegant. Apply GEO structure to existing brand-voiced content, not as a separate "SEO layer."

   **Skill reference:** For detailed GEO patterns and the SEO-Emotional Arc beat mapping, consult `skills/structured-data/SKILL.md` Layer 3 (the GEO integration section).
   ```

**C. design-system-scaffold/SKILL.md -- Add SEO scaffold items to Wave 0:**

1. Read the existing file. Find the "Wave 0 Scaffold Task List" section (around line 654). In the task list block (between the `<tasks>` markers around lines 659-669), add SEO scaffold items AFTER the existing tasks but BEFORE the closing `</tasks>`:

   ```markdown
   - [auto] Create app/sitemap.ts with dynamic sitemap generation from page routes (reference seo-meta skill)
   - [auto] Create app/robots.ts with sitemap reference and crawler directives (reference seo-meta skill)
   - [auto] Add metadataBase to app/layout.tsx metadata export (reference seo-meta skill)
   - [auto] Create app/opengraph-image.tsx root-level default OG image with DNA tokens (reference og-images skill)
   ```

   For Astro projects, add an alternative note after the task list:
   ```markdown
   **Astro variant:** Replace Next.js-specific files:
   - `app/sitemap.ts` -> `@astrojs/sitemap` integration in `astro.config.mjs`
   - `app/robots.ts` -> `public/robots.txt` static file
   - `app/opengraph-image.tsx` -> `src/pages/og/default.png.ts` endpoint
   - metadataBase -> BaseLayout `<head>` meta tags
   ```

2. In the Layer 1 "What Gets Generated (File Manifest)" table (around lines 24-34), add SEO rows to the table:

   | File | Purpose | Source |
   |------|---------|--------|
   | `app/sitemap.ts` | Dynamic sitemap generation | Page routes + seo-meta skill |
   | `app/robots.ts` | Crawler directives with sitemap reference | seo-meta skill |
   | `app/opengraph-image.tsx` | Root-level default OG image | DNA tokens + og-images skill |

3. In the Layer 3 "Related Skills" section (around line 699-704), add the v1.5 skill references:

   ```markdown
   - `seo-meta` -- Provides sitemap, robots.txt, and metadata patterns scaffolded in Wave 0
   - `og-images` -- Provides OG image generation patterns for the root-level opengraph-image.tsx
   - `structured-data` -- Section builders may need to add JSON-LD scripts per section PLAN.md schema_type
   ```
  </action>
  <verify>
Read all three modified files. Confirm:
1. researcher.md frontmatter tools list includes mcp__context7__resolve-library-id and mcp__context7__query-docs
2. researcher.md has Context7 MCP Integration section with 3-step usage pattern
3. researcher.md has fallback chain documented (Context7 -> WebFetch -> WebSearch)
4. researcher.md Rules section has "Context7 first for libraries" rule
5. content-specialist.md has GEO Content Patterns section with 4 patterns (BLUF, question headings, quotable stats, FAQ-first)
6. content-specialist.md GEO section has archetype-aware note
7. design-system-scaffold SKILL.md Wave 0 task list includes 4 SEO items (sitemap, robots, metadataBase, OG image)
8. design-system-scaffold has Astro variant note
9. design-system-scaffold file manifest table includes SEO rows
10. design-system-scaffold Related Skills includes seo-meta, og-images, structured-data
11. All changes are ADDITIVE -- no existing content removed
  </verify>
  <done>Researcher has Context7 MCP tools, content-specialist has GEO pattern awareness, and design-system-scaffold Wave 0 now includes SEO files. All v1.5 skills are wired into the pipeline.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. section-planner assigns schema_type and og_template in PLAN.md frontmatter
2. quality-reviewer has SEO checklist with CRITICAL/WARNING severity
3. build-orchestrator has Wave 0 SEO scaffold items and spawn prompt SEO context
4. researcher has Context7 MCP tools with usage guidance and fallback chain
5. content-specialist has GEO content patterns (BLUF, question headings, quotable stats)
6. design-system-scaffold Wave 0 includes SEO files (sitemap, robots, metadataBase, OG image)
7. All 6 files modified are syntactically correct markdown (no broken formatting)
8. All changes are additive -- no existing behavior removed
9. No circular references introduced between agents

**Agent wiring coverage:**
- section-planner -> structured-data (schema assignment), og-images (template type)
- quality-reviewer -> seo-meta (validation values), structured-data (schema types)
- build-orchestrator -> seo-meta (scaffold patterns), og-images (OG route scaffold)
- researcher -> Context7 MCP (live documentation)
- content-specialist -> structured-data (GEO patterns)
- design-system-scaffold -> seo-meta (sitemap, robots), og-images (OG route)
</verification>

<success_criteria>
- All 6 files have targeted additions with no existing content removed
- Section-planner generates PLAN.md with schema_type and og_template fields
- Quality-reviewer SEO checklist has 6 CRITICAL and 5 WARNING items
- Build-orchestrator Wave 0 includes sitemap, robots, metadataBase, OG route
- Researcher tools list includes Context7 MCP tools with usage guidance
- Content-specialist has GEO patterns with archetype-aware note
- Design-system-scaffold Wave 0 task list includes 4 SEO items with Astro variant
</success_criteria>

<output>
After completion, create `.planning/phases/18-dynamic-og-images-pipeline-wiring/18-03-SUMMARY.md`
</output>
