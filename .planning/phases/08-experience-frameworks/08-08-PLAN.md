---
phase: 08-experience-frameworks
plan: 08
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06", "08-07"]
files_modified:
  - skills/ecommerce-ui/SKILL.md
  - skills/dashboard-patterns/SKILL.md
  - skills/blog-patterns/SKILL.md
  - skills/portfolio-patterns/SKILL.md
  - skills/seo-meta/SKILL.md
  - skills/i18n-rtl/SKILL.md
  - skills/form-builder/SKILL.md
autonomous: true

must_haves:
  truths:
    - "All rewritten skills use the 4-layer format (Decision Guidance, Award-Winning Examples, Integration Context, Anti-Patterns)"
    - "All code examples use current library versions (Tailwind v4 @theme, Motion 12.x motion/react, GSAP 3.14)"
    - "No v3 Tailwind patterns, no deprecated framework APIs, no framer-motion imports in any rewritten skill"
    - "Each skill references DNA integration, archetype variants, and cross-skill connections"
    - "TypeScript strict mode and clean component patterns are the standard in all code examples"
  artifacts:
    - path: "skills/ecommerce-ui/SKILL.md"
      provides: "E-commerce UI patterns rewritten to 4-layer format with current library versions"
      contains: "Layer 1: Decision Guidance"
    - path: "skills/dashboard-patterns/SKILL.md"
      provides: "Dashboard patterns rewritten to 4-layer format"
      contains: "Layer 1: Decision Guidance"
    - path: "skills/blog-patterns/SKILL.md"
      provides: "Blog patterns rewritten to 4-layer format"
      contains: "Layer 1: Decision Guidance"
    - path: "skills/portfolio-patterns/SKILL.md"
      provides: "Portfolio patterns rewritten to 4-layer format"
      contains: "Layer 1: Decision Guidance"
    - path: "skills/seo-meta/SKILL.md"
      provides: "SEO meta patterns rewritten to 4-layer format"
      contains: "Layer 1: Decision Guidance"
    - path: "skills/i18n-rtl/SKILL.md"
      provides: "i18n/RTL patterns rewritten to 4-layer format"
      contains: "Layer 1: Decision Guidance"
    - path: "skills/form-builder/SKILL.md"
      provides: "Form patterns rewritten to 4-layer format"
      contains: "Layer 1: Decision Guidance"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "skills/tailwind-system"
      via: "All rewritten skills reference Tailwind v4 @theme patterns"
      pattern: "@theme|tailwind-system"
    - from: "skills/*/SKILL.md"
      to: "skills/accessibility"
      via: "All rewritten skills include ARIA, keyboard, focus patterns"
      pattern: "accessibility|WCAG|aria"
---

<objective>
Rewrite surviving domain and utility skills to the 4-layer format with current library versions. This is the SKIL-04 requirement: every surviving skill rewritten to the new quality standard.

Purpose: v6.1.0 skills use outdated patterns (Tailwind v3, framer-motion, Next.js 14 APIs). They lack the 4-layer structure, DNA integration context, and archetype variant guidance. This plan rewrites the most important surviving skills that were not already rewritten by earlier phases.
Output: 7 rewritten SKILL.md files in existing skill directories
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-experience-frameworks/08-CONTEXT.md
@.planning/phases/08-experience-frameworks/08-RESEARCH.md
@skills/_skill-template/SKILL.md
@skills/ecommerce-ui/SKILL.md
@skills/dashboard-patterns/SKILL.md
@skills/blog-patterns/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite domain skills to 4-layer format</name>
  <files>skills/ecommerce-ui/SKILL.md, skills/dashboard-patterns/SKILL.md, skills/blog-patterns/SKILL.md, skills/portfolio-patterns/SKILL.md</files>
  <action>
Rewrite these 4 domain skills IN-PLACE (same directory, replace content completely). Each gets the 4-layer format with current library versions. Target 250-350 lines each.

**For each skill, the rewrite must:**

1. Add YAML frontmatter: name, description, tier ("domain"), triggers, version "2.0.0"

2. **Layer 1: Decision Guidance** (~40-60 lines)
   - When to use (what project types)
   - When NOT to use
   - Decision tree for common choices within the domain
   - Connection to pipeline stages

3. **Layer 2: Award-Winning Examples** (~100-150 lines)
   - 3-5 copy-paste TSX patterns using:
     - Tailwind v4 classes (NOT v3 patterns)
     - DNA tokens (`bg-bg`, `text-text`, `border-border`, `bg-primary`, etc.)
     - `motion/react` for animations (NOT `framer-motion`)
     - Container queries where appropriate (`@container`, `@sm:`, `@md:`)
     - Accessible patterns (ARIA attributes, keyboard handling, focus indicators)
     - TypeScript strict types
   - 2-3 reference sites (real award-winning examples with what makes them excellent)

4. **Layer 3: Integration Context** (~30-40 lines)
   - DNA Connection: which tokens map to this domain
   - Archetype Variants: how patterns change per archetype
   - Related Skills cross-references

5. **Layer 4: Anti-Patterns** (~20-30 lines)
   - 3-4 anti-patterns specific to this domain
   - All must reference the CORRECT way (not just "don't do X")

**Specific guidance per skill:**

**ecommerce-ui:**
- Product cards, product grids, cart UI, checkout flow
- Archetype variants: Luxury = minimal grid, Playful = dynamic cards, Swiss = clean catalog
- Must include: container query product cards, accessible add-to-cart, price formatting
- Anti-patterns: fixed-width product grids, inaccessible quantity steppers, checkout without progress indication

**dashboard-patterns:**
- Data-dense layouts, metric cards, chart integration areas, sidebar nav
- Archetype variants: Data-Dense = compact grid, Neo-Corporate = clean panels, AI-Native = conversational
- Must include: container query metric cards, responsive sidebar/overlay, accessible data tables
- Anti-patterns: viewport-based card layouts (use container queries), fixed sidebar on mobile

**blog-patterns:**
- Article layout, typography, reading experience, blog index, tag/category pages
- Archetype variants: Editorial = magazine layout, Japanese Minimal = clean reading, Brutalist = raw text
- Must include: responsive article typography (hybrid system from responsive-design skill), accessible table of contents, dark mode reading comfort
- Anti-patterns: fixed-width article text, missing skip-to-content, no dark mode consideration

**portfolio-patterns:**
- Project showcase, gallery, case study layout, creative grid
- Archetype variants: Luxury = curated minimal, Kinetic = animated showcase, Brutalist = raw grid
- Must include: container query project cards, accessible gallery with keyboard nav, motion-safe hover effects
- Anti-patterns: autoplay everything, inaccessible lightbox, no keyboard navigation in gallery
  </action>
  <verify>
- All 4 skill files exist and are 250+ lines each
- Each has valid YAML frontmatter with tier "domain" and version "2.0.0"
- Each has all 4 layer headings
- NONE contain `framer-motion` imports (must use `motion/react`)
- NONE contain `tailwind.config.ts` as recommended pattern
- ALL contain DNA token references (bg-bg, text-text, bg-primary, etc.)
- ALL contain at least one container query example
- ALL contain at least one ARIA attribute in code examples
- ALL contain `motion-safe:` or `motion-reduce:` in animation examples
  </verify>
  <done>
- 4 domain skills rewritten to 4-layer format with current libraries
- All code examples use Tailwind v4, motion/react, TypeScript strict
- DNA integration, archetype variants, and cross-skill references in every skill
- Accessible patterns baked into all code examples
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite utility skills to 4-layer format</name>
  <files>skills/seo-meta/SKILL.md, skills/i18n-rtl/SKILL.md, skills/form-builder/SKILL.md</files>
  <action>
Rewrite these 3 utility skills IN-PLACE. Same 4-layer format requirements as Task 1. Target 200-300 lines each (utility skills are more focused).

**For each skill, same rewrite template as Task 1 but with tier "utility".**

**Specific guidance per skill:**

**seo-meta:**
- Meta tags, structured data, Open Graph, Twitter cards, canonical URLs, sitemap
- Framework-specific: Next.js metadata API (generateMetadata), Astro `<head>` management, React/Vite with react-helmet-async
- Must include: Next.js 16 metadata patterns (NOT older Head component), Astro frontmatter metadata, proper og:image handling
- DNA Connection: Title formatting, brand-consistent descriptions
- Anti-patterns: duplicate meta tags, missing og:image, hardcoded canonical URLs, no framework-specific metadata API usage

**i18n-rtl:**
- Internationalization, right-to-left layout support, locale-aware formatting
- Must include: CSS logical properties (`margin-inline-start` instead of `margin-left`), `dir="rtl"` on `<html>`, Tailwind logical utilities, locale-aware number/date formatting
- DNA Connection: RTL-aware spacing, bidirectional text handling
- Archetype variants: Most archetypes work the same in RTL, but asymmetric layouts (Editorial, Brutalist) need specific RTL mirroring rules
- Anti-patterns: physical properties instead of logical, left/right instead of start/end, ignoring text direction in animations

**form-builder:**
- Form layouts, input components, validation UI, error states, multi-step forms
- Must include: accessible form patterns (label association, error announcements, field descriptions), container query form layouts, DNA-styled inputs, motion-safe validation feedback
- Archetype variants: Swiss = clean aligned forms, Brutalist = raw unstyled inputs, Luxury = minimal floating labels
- Anti-patterns: placeholder as label, error messages without aria-live, color-only error indication, submit buttons without loading state
  </action>
  <verify>
- All 3 skill files exist and are 200+ lines each
- Each has valid YAML frontmatter with tier "utility" and version "2.0.0"
- Each has all 4 layer headings
- seo-meta: Contains `generateMetadata` (Next.js 16 pattern), NOT `Head` component
- i18n-rtl: Contains CSS logical properties (`inline-start`, `inline-end`)
- form-builder: Contains `aria-describedby` or `aria-live` (accessible form patterns)
- NONE contain outdated library references
- ALL contain DNA token references
  </verify>
  <done>
- 3 utility skills rewritten to 4-layer format with current libraries
- SEO patterns use framework-specific metadata APIs (not legacy patterns)
- i18n/RTL uses CSS logical properties throughout
- Form patterns are fully accessible with proper error handling
  </done>
</task>

</tasks>

<verification>
- All 7 skills rewritten with 4-layer format and version 2.0.0
- No v3 Tailwind, no framer-motion, no deprecated Next.js APIs in any skill
- All code examples use DNA tokens, container queries, accessible patterns
- TypeScript strict in all code examples
- Domain skills (250+ lines each), utility skills (200+ lines each)
</verification>

<success_criteria>
1. Every rewritten skill follows the 4-layer format exactly
2. No outdated library patterns survive in any rewritten skill
3. DNA integration context is clear in every skill
4. A Claude executor can reference any skill and produce correct, accessible, current code
</success_criteria>

<output>
After completion, create `.planning/phases/08-experience-frameworks/08-08-SUMMARY.md`
</output>
