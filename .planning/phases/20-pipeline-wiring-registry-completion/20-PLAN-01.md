---
phase: 20-pipeline-wiring-registry-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/SKILL-DIRECTORY.md
  - agents/pipeline/build-orchestrator.md
  - agents/pipeline/section-planner.md
  - agents/pipeline/quality-reviewer.md
  - skills/emotional-arc/SKILL.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "api-patterns and ssr-dynamic-content appear in SKILL-DIRECTORY.md with correct line counts, tiers, and descriptions"
    - "SKILL-DIRECTORY.md shows 51 total v2.0 (22 Core + 26 Domain + 2 Utility + 1 template) and Domain count is 26"
    - "Build-orchestrator Wave 0 scaffold list includes IndexNow endpoint, key file, robots.txt AI presets, llms.txt, next.config.ts cache setup, proxy.ts, and loading.tsx skeletons"
    - "Section-planner PLAN.md frontmatter includes integration_type and rendering_strategy fields"
    - "Quality-reviewer has env secret exposure checks, SSR anti-pattern checks, and schema-content audit step"
    - "Emotional-arc Related Skills section references structured-data SEO beat mapping"
  artifacts:
    - path: "skills/SKILL-DIRECTORY.md"
      provides: "Complete v1.5 skill registry with api-patterns and ssr-dynamic-content"
      contains: "api-patterns"
    - path: "agents/pipeline/build-orchestrator.md"
      provides: "Expanded Wave 0 scaffold with search-visibility and ssr-dynamic-content items"
      contains: "indexnow"
    - path: "agents/pipeline/section-planner.md"
      provides: "integration_type and rendering_strategy assignment logic"
      contains: "integration_type"
    - path: "agents/pipeline/quality-reviewer.md"
      provides: "Env secret checks, SSR anti-pattern checks, schema audit protocol"
      contains: "NEXT_PUBLIC_"
    - path: "skills/emotional-arc/SKILL.md"
      provides: "Back-reference to structured-data SEO beat mapping"
      contains: "structured-data"
  key_links:
    - from: "agents/pipeline/section-planner.md"
      to: "skills/api-patterns/SKILL.md"
      via: "skill reference directive"
      pattern: "api-patterns"
    - from: "agents/pipeline/section-planner.md"
      to: "skills/ssr-dynamic-content/SKILL.md"
      via: "skill reference directive"
      pattern: "ssr-dynamic-content"
    - from: "agents/pipeline/quality-reviewer.md"
      to: "skills/api-patterns/SKILL.md"
      via: "env secret check reference"
      pattern: "api-patterns"
---

<objective>
Wire api-patterns and ssr-dynamic-content into the agent pipeline and skill registry, closing GAP-1 through GAP-5 from the v1.5 milestone audit.

Purpose: These two skills (Phase 17 and Phase 19) were created but never integrated into the pipeline agents that discover, load, and enforce them during builds. Without this wiring, projects needing API forms, webhooks, SSR, or dynamic content would not automatically get the right patterns.

Output: Updated SKILL-DIRECTORY.md, build-orchestrator.md, section-planner.md, quality-reviewer.md, and emotional-arc SKILL.md -- all additive markdown edits following Phase 18 patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL-DIRECTORY.md registration + emotional-arc back-reference (GAP-1 + Success Criterion 6)</name>
  <files>skills/SKILL-DIRECTORY.md, skills/emotional-arc/SKILL.md</files>
  <action>
**SKILL-DIRECTORY.md changes (all in skills/SKILL-DIRECTORY.md):**

1. In the "Integration" subsection of Domain Skills (after the `og-images` row at line 133), add two new rows:

```
| `api-patterns` | COMPLETE | 17 | 1601 | Server-side API integration: Context7 MCP for live docs, server-side proxies with env secret protection, CRM form patterns (HubSpot, Salesforce), typed API clients with discriminated unions, webhook receivers with signature verification, Cloudflare Turnstile spam protection |
| `ssr-dynamic-content` | COMPLETE | 19 | 1842 | SSR/ISR/streaming decision guidance: 4-dimension rendering matrix, Cache Components (Next.js 16), Server Islands (Astro 5), CMS webhook revalidation for 5 platforms, auth-gated rendering for 4 libraries, cache invalidation strategies, loading state patterns |
```

2. Update line 12 table: Change Domain column from `24 skills` to `26 skills`.

3. Update the Skill Count Summary table (around line 201-211):
   - Domain row: change `24` to `26`
   - Total v2.0 row: change `49` to `51`, change `48 skills + 1 template` to `50 skills + 1 template`
   - Total in filesystem row: change `76` to `78`, change `49 v2.0 + 27 legacy` to `51 v2.0 + 27 legacy`

4. Also update the header paragraph on line 15 that says "Loading all 48 skills" to "Loading all 50 skills".

5. Update the footer (around line 315-317):
   - Change registry version from `2.1.0` to `2.2.0`
   - Change "Last updated" to `Phase 20, Plan 1 -- v1.5 gap closure (api-patterns + ssr-dynamic-content registration)`
   - Change total from `49 (22 core, 24 domain, 2 utility + 1 template)` to `51 (22 core, 26 domain, 2 utility + 1 template)`

**emotional-arc SKILL.md changes (in skills/emotional-arc/SKILL.md):**

6. In the "Related Skills" section (around lines 612-619), add a new entry after the `cinematic-motion` bullet:

```
- **structured-data** -- SEO beat mapping: HOOK = H1 + primary keyword schema, PROOF = statistics + schema markup, TENSION = FAQ + FAQPage schema; consult structured-data skill Layer 3 for the full beat-to-schema mapping table
```

This creates the bi-directional reference: structured-data already references emotional-arc, and now emotional-arc references structured-data back.
  </action>
  <verify>
1. `grep -c "api-patterns" skills/SKILL-DIRECTORY.md` returns at least 1
2. `grep -c "ssr-dynamic-content" skills/SKILL-DIRECTORY.md` returns at least 1
3. `grep "26 skills" skills/SKILL-DIRECTORY.md` matches Domain count
4. `grep "51" skills/SKILL-DIRECTORY.md` matches total v2.0 count
5. `grep "2.2.0" skills/SKILL-DIRECTORY.md` matches new registry version
6. `grep "structured-data" skills/emotional-arc/SKILL.md` returns at least 1
  </verify>
  <done>
SKILL-DIRECTORY.md lists api-patterns (1601 lines, Phase 17) and ssr-dynamic-content (1842 lines, Phase 19) in the Integration subsection. Domain count is 26, total v2.0 is 51 (50 skills + 1 template). Registry version is 2.2.0. Emotional-arc has a back-reference to structured-data SEO beat mapping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Agent pipeline wiring -- build-orchestrator, section-planner, quality-reviewer (GAP-2 through GAP-5)</name>
  <files>agents/pipeline/build-orchestrator.md, agents/pipeline/section-planner.md, agents/pipeline/quality-reviewer.md</files>
  <action>
**build-orchestrator.md changes (GAP-2):**

1. Replace the Wave 0 SEO scaffold block at lines 72-76 with an expanded version. Keep the existing 4 items and ADD new items. The new block should read:

```markdown
**Wave 0 SEO scaffold:** When executing Wave 0, the scaffold builder also generates SEO foundation files alongside design tokens. Reference the `seo-meta` and `og-images` skills for scaffold patterns. SEO scaffold items:
- `app/sitemap.ts` or `@astrojs/sitemap` config (sitemap generation)
- `app/robots.ts` or `public/robots.txt` (crawler directives)
- Metadata base configuration in root layout (metadataBase for Next.js)
- `app/opengraph-image.tsx` root-level default OG image (Next.js) or `src/pages/og/` directory (Astro)

**Wave 0 search visibility scaffold:** Reference the `search-visibility` skill for these patterns:
- `app/api/indexnow/route.ts` IndexNow submission endpoint (Next.js) or `src/pages/api/indexnow.ts` (Astro)
- IndexNow key file in `public/` directory (auto-generated UUID)
- `robots.txt` AI crawler preset (search-visibility skill defines 3 tiers: permissive, balanced, restrictive)
- `public/llms.txt` site description for AI crawlers (if project has substantial content)

**Wave 0 dynamic content scaffold:** Reference the `ssr-dynamic-content` skill when the project uses dynamic data, CMS content, or authentication:
- `next.config.ts` cache configuration (cacheLife profiles for ISR/on-demand) -- Next.js only
- `proxy.ts` route protection setup -- if project requires authentication (Next.js 16 pattern, replaces middleware.ts)
- Skeleton `loading.tsx` files for routes with dynamic data (one per dynamic route segment)
```

**section-planner.md changes (GAP-3 + GAP-4):**

2. After the existing skill reference directives (around line 22, after the structured-data skill reference), add two new skill reference blocks:

```markdown
**Skill reference:** Load `skills/api-patterns/SKILL.md` Layer 1 decision tree when a section involves form submission, external API calls, webhook receivers, or CRM integration. Assign the `integration_type` field in the PLAN.md frontmatter based on the skill's integration type categories: `form-submission`, `api-client`, `webhook-receiver`, `email-send`, or `none`.

**Skill reference:** Load `skills/ssr-dynamic-content/SKILL.md` Layer 1 rendering matrix when a section has dynamic data (CMS content, database queries, user-specific content) or requires authentication. Assign the `rendering_strategy` field in the PLAN.md frontmatter based on the skill's 4-dimension decision matrix: `static`, `isr`, `ssr`, `streaming`, or `hybrid`.
```

3. In the PLAN.md frontmatter format (around lines 117-135), add two new fields after the `og_template` field:

```yaml
integration_type: [form-submission | api-client | webhook-receiver | email-send | none]
rendering_strategy: [static | isr | ssr | streaming | hybrid]
```

4. After the SEO fields explanation paragraph (around line 138), add a new paragraph:

```markdown
**Integration fields:** `integration_type` is determined by consulting the `api-patterns` skill's Layer 1 decision tree based on whether the section needs form handling, external API calls, or webhook processing. Set to `none` if no API integration applies. `rendering_strategy` is determined by consulting the `ssr-dynamic-content` skill's Layer 1 rendering matrix based on data freshness needs, auth requirements, and content source. Set to `static` if no dynamic content applies. Both fields default to `none`/`static` respectively for pure static marketing sections.
```

**quality-reviewer.md changes (GAP-3 + GAP-4 + GAP-5):**

5. After the SEO Verification Checklist section (after line 118, before the `---` separator and Anti-Slop section), add three new verification sections:

```markdown
### Environment Secret Exposure Check

After the SEO checklist, verify env variable security on all sections with `integration_type` set (from PLAN.md frontmatter). This check is CRITICAL -- exposed secrets are a security vulnerability, not a style issue.

- [ ] No `NEXT_PUBLIC_` prefixed variables contain API secrets (Stripe secret key, database URLs, webhook signing secrets)
- [ ] No `PUBLIC_` (Astro) or `VITE_` (Vite) prefixed variables contain server-only secrets
- [ ] All external API calls route through server-side handlers (server actions, API routes, Astro endpoints) -- never direct client-side fetch to external APIs with auth headers
- [ ] `.env.example` exists documenting required variables without actual values
- [ ] Webhook signature verification is present on all webhook receiver endpoints (HMAC or provider-specific method)

**Skill reference for validation:** Consult `api-patterns` skill Layer 4 anti-patterns for the complete list of env exposure failure modes.

### SSR Anti-Pattern Check

For sections with `rendering_strategy` set (from PLAN.md frontmatter), verify correct SSR/caching implementation. These checks catch silent failures that produce no errors but serve stale or incorrect content.

**CRITICAL (silent failure patterns):**
- [ ] No `experimental.ppr` in next.config.ts (deprecated in Next.js 16 -- use Cache Components instead)
- [ ] No `unstable_cache` usage (removed in Next.js 16 -- use `cacheLife`/`cacheTag`/`updateTag` instead)
- [ ] No auth checks in `middleware.ts` (Next.js 16 uses `proxy.ts` for route protection -- middleware.ts auth causes redirect loops)
- [ ] No `getSession()` for auth validation (use `getClaims()` or equivalent server-side token verification -- `getSession()` trusts client JWT without server verification)

**WARNING (suboptimal patterns):**
- [ ] Cache headers include `stale-while-revalidate` for ISR pages
- [ ] Loading skeletons exist for routes with `rendering_strategy: ssr` or `streaming`
- [ ] Draft mode / preview mode properly isolated from production cache

**Skill reference for validation:** Consult `ssr-dynamic-content` skill Layer 4 anti-patterns for the complete list of deprecated patterns and their replacements.

### Schema-Content Consistency Audit (SDATA-06)

After SEO and security checks, run the schema-content audit on every page with JSON-LD structured data. This prevents Google manual actions for mismatched schema claims. Load the `structured-data` skill and execute SDATA-06 protocol.

**CRITICAL (Google manual action risk):**
- [ ] Every `name` field in JSON-LD matches visible page heading text
- [ ] Every `description` field in JSON-LD matches visible page description or meta description
- [ ] FAQPage `acceptedAnswer` text matches visible answer content on the page
- [ ] Product `price` and `availability` match visible product information
- [ ] Article `datePublished` and `dateModified` match visible date displays
- [ ] No schema types claimed that have zero visible content (e.g., FAQPage schema on a page with no FAQ section)
- [ ] `@graph` combinations do not double-declare the same entity

**WARNING:**
- [ ] Organization schema `logo` URL resolves to an actual image
- [ ] BreadcrumbList matches the visible breadcrumb navigation
- [ ] Review/Rating `ratingValue` matches visible star display

**How to use:** Run this audit AFTER the SEO checklist and BEFORE anti-slop scoring. Schema-content mismatches are CRITICAL findings that generate GAP-FIX.md entries. This audit runs on every quality-reviewer pass, not just the final review.
```

All changes are strictly ADDITIVE. Do NOT remove or modify any existing content in these three agent files. Only append new sections or insert new lines at the specified locations.
  </action>
  <verify>
1. `grep -c "indexnow" agents/pipeline/build-orchestrator.md` (case-insensitive) returns at least 1
2. `grep -c "proxy.ts" agents/pipeline/build-orchestrator.md` returns at least 1
3. `grep -c "loading.tsx" agents/pipeline/build-orchestrator.md` returns at least 1
4. `grep "integration_type" agents/pipeline/section-planner.md` returns matches
5. `grep "rendering_strategy" agents/pipeline/section-planner.md` returns matches
6. `grep "api-patterns" agents/pipeline/section-planner.md` returns at least 1
7. `grep "ssr-dynamic-content" agents/pipeline/section-planner.md` returns at least 1
8. `grep "NEXT_PUBLIC_" agents/pipeline/quality-reviewer.md` returns at least 1
9. `grep "experimental.ppr" agents/pipeline/quality-reviewer.md` returns at least 1
10. `grep "unstable_cache" agents/pipeline/quality-reviewer.md` returns at least 1
11. `grep "middleware.ts" agents/pipeline/quality-reviewer.md` returns at least 1
12. `grep "SDATA-06" agents/pipeline/quality-reviewer.md` returns at least 1
13. `grep "Schema-Content Consistency" agents/pipeline/quality-reviewer.md` returns at least 1
  </verify>
  <done>
Build-orchestrator Wave 0 includes search-visibility items (IndexNow, key file, AI presets, llms.txt) and ssr-dynamic-content items (next.config.ts cache, proxy.ts, loading.tsx). Section-planner assigns integration_type and rendering_strategy with skill reference directives. Quality-reviewer includes env secret exposure checks, SSR anti-pattern checks (experimental.ppr, unstable_cache, middleware.ts, getSession), and SDATA-06 schema-content audit protocol. All changes are additive -- no existing behavior removed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full gap closure:

1. **GAP-1 (SKILL-DIRECTORY):** `grep "api-patterns\|ssr-dynamic-content" skills/SKILL-DIRECTORY.md` shows both skills registered
2. **GAP-2 (Wave 0):** `grep -i "indexnow\|proxy.ts\|loading.tsx\|llms.txt\|cache" agents/pipeline/build-orchestrator.md` shows all new scaffold items
3. **GAP-3 (api-patterns wiring):** `grep "api-patterns" agents/pipeline/section-planner.md agents/pipeline/quality-reviewer.md` shows skill referenced in both agents
4. **GAP-4 (ssr-dynamic-content wiring):** `grep "ssr-dynamic-content" agents/pipeline/section-planner.md agents/pipeline/quality-reviewer.md` shows skill referenced in both agents
5. **GAP-5 (Schema audit):** `grep "SDATA-06" agents/pipeline/quality-reviewer.md` shows audit protocol embedded
6. **Success Criterion 6:** `grep "structured-data" skills/emotional-arc/SKILL.md` shows back-reference exists
</verification>

<success_criteria>
- api-patterns and ssr-dynamic-content are registered in SKILL-DIRECTORY.md under Integration subsection with correct line counts (1601 and 1842)
- Domain skill count is 26, total v2.0 is 51, registry version is 2.2.0
- Build-orchestrator has 3 Wave 0 scaffold blocks: SEO (existing), search visibility (new), dynamic content (new)
- Section-planner has integration_type and rendering_strategy in PLAN.md frontmatter template
- Section-planner has skill reference directives for api-patterns and ssr-dynamic-content
- Quality-reviewer has 3 new check sections: env secret exposure, SSR anti-patterns, schema-content audit
- Emotional-arc has structured-data in its Related Skills section
- All changes are additive -- no existing behavior removed or modified
</success_criteria>

<output>
After completion, create `.planning/phases/20-pipeline-wiring-registry-completion/20-01-SUMMARY.md`
</output>
