{
  "gpt-image": {
    "command": "node",
    "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-servers/gpt-image/index.mjs"],
    "env": {
      "OPENAI_API_KEY": "${OPENAI_API_KEY}",
      "GPT_IMAGE_OUTPUT_DIR": "${GPT_IMAGE_OUTPUT_DIR:-}",
      "GPT_IMAGE_MODEL": "${GPT_IMAGE_MODEL:-gpt-image-2}"
    },
    "description": "OpenAI gpt-image-2 image generation + editing (generate_image, edit_image).",
    "optional": true
  },
  "stitch": {
    "command": "npx",
    "args": ["-y", "@anthropic-ai/stitch-mcp@latest"],
    "description": "Visual UI design generation (Google Stitch). Text-to-screen mockups, design system management, variant generation with creative range control (REFINE/EXPLORE/REIMAGINE).",
    "optional": true
  },
  "playwright": {
    "command": "npx",
    "args": ["-y", "@anthropic-ai/playwright-mcp@latest"],
    "description": "Browser automation for visual QA. 4-breakpoint screenshots, CSS/DOM evaluation, accessibility auditing, console error detection, interaction testing.",
    "optional": true
  },
  "chrome-devtools": {
    "command": "npx",
    "args": ["-y", "chrome-devtools-mcp@latest"],
    "description": "Chrome DevTools MCP (official, by Chrome team). Exposes performance traces, network, console, DOM inspection, Lighthouse-style perf analysis on a live Chrome. Requires Node 20.19+. Complements Playwright for perf/tracing where Playwright snapshots fall short. Used by motion-health and perf-budgets skills.",
    "optional": true
  },
  "next-devtools": {
    "command": "npx",
    "args": ["-y", "next-devtools-mcp@latest"],
    "description": "Next.js DevTools MCP (official Vercel). Exposes Next.js build logs, deployment info, routing inspection. Separate from vercel-plugin MCP — focused on Next.js runtime introspection, not deploys.",
    "optional": true
  },
  "3dsvg-export": {
    "command": "node",
    "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-servers/3dsvg-export/server.mjs"],
    "description": "Headless 3dsvg export server (Genorah v3.4.1). Renders preset → PNG (single, batch) and MP4 via Playwright + 3dsvg registerCanvas. Used by /gen:hero-mark export for the 30-variant brand matrix. Requires playwright (already declared) + optional @ffmpeg-installer/ffmpeg for MP4.",
    "optional": true
  },
  "flux-kontext": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@fal/flux-kontext-mcp@^1.0.0"],
    "env": { "FAL_KEY": "${FAL_KEY}" },
    "description": "Flux Kontext image generation via fal.ai. DNA-locked hero backgrounds with reference image conditioning. Optional — pipeline uses HTTP adapter when MCP is absent.",
    "optional": true
  },
  "recraft": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@recraft/mcp-server@^1.0.0"],
    "env": { "RECRAFT_API_KEY": "${RECRAFT_API_KEY}" },
    "description": "Recraft V3 native SVG vector generation. Brand marks, favicon, OG templates with DNA-token color binding. Optional — pipeline uses HTTP adapter when MCP is absent.",
    "optional": true
  },
  "graphify": {
    "type": "stdio",
    "command": "graphify-mcp",
    "args": ["graphify-out/graph.json"],
    "description": "Graphify knowledge-graph MCP — query_graph, get_node, get_neighbors, shortest_path over the repo's AST + semantic graph. Optional — recall() falls back to BM25 when absent.",
    "optional": true
  }
}
